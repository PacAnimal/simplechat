[lint]
select = ["E", "W", "F", "I", "UP"]
ignore = [
    "E501",   # line too long — handled by formatter
]

[lint.per-file-ignores]
"tests/**" = ["F811"]  # redefined-unused OK in test fixtures

[format]
quote-style = "double"
indent-style = "space"
